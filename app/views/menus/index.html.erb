<h1><%= @restaurant.name %> - Menus</h1>

<ul>
  <% @menus.each do |menu| %>
    <li><%= link_to menu.name, restaurant_menu_path(@restaurant, menu) %></li>
  <% end %>
</ul>

<%= link_to 'Add New Menu', new_restaurant_menu_path(@restaurant), class: 'button' %>
